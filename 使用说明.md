# DwgSearch 使用说明

> 适用版本：V2.22　｜　系统：Windows 7 / 10 / 11

DwgSearch 用来**批量搜索、批量替换 DWG 图纸里的文件名和文字内容**。它先在后台把图纸文字建成本地索引，之后搜索只查索引，不用逐个打开图纸，所以很快。

---

## 1. 三步上手

1. **启动软件。** 启动约 3 秒后自动开始后台索引（状态栏显示「后台索引中」）。
2. **输入关键词。** 在「搜索文件名」和/或「搜索文件内容」框里输入，按回车或点放大镜。
3. **看结果。** 单击一行，右侧显示图纸文字（命中词高亮）和缩略图；双击打开图纸。

**界面布局：** 顶部是两个搜索框和「筛选」按钮 → 下面一行是搜索目录标签 → 左侧结果表格 → 右侧上方文字预览、下方缩略图 → 底部状态栏。

---

## 2. 搜索

### 关键词规则

| 项目 | 规则 |
|---|---|
| 多个关键词 | 用空格隔开，**必须同时满足**。内容搜索是指"这份图纸里出现过这些词"，不要求在同一行 |
| 文件名 + 内容 | 两个框都填，则两个条件同时满足 |
| 最短长度 | 每个词至少 **2 个字符**，太短的词会被忽略（文件名里的通配符 `*` `?` 不计入字数） |
| 大小写 | 不区分 |
| 匹配方式 | 包含即可：关键词出现在文件名 / 图纸文字的任意位置就算命中 |
| 通配符（仅文件名） | `*` 匹配任意多个字符（含 0 个），`?` 匹配恰好 1 个字符。例：`N-3*装配图`、`图纸?.dwg`、`M-12*UHF*01`。**内容搜索不支持通配符**（图纸文字里的 `*` `?` 按普通字符处理） |
| 正则 | 点输入框右侧的 `.*` 图标开启。开启后整个框作为**一条**正则，不再按空格拆词；写法有误会弹窗提示 |
| 历史记录 | 点输入框右侧的下拉箭头可选历史关键词，列表里有「清空记录」可一键清除 |

### 搜索目录（第二行的标签）

- 点空白处或「+」添加目录，也可以 `Ctrl+V` 粘贴路径。
- **勾选的目录 = 搜索范围；一个都不勾 = 搜全部。** 多个目录之间是"或"的关系。
- 标签操作：单击选中，再单击一次可编辑路径，双击在资源管理器中打开，鼠标悬停出现 × 可移除，可拖动调整顺序。

### 筛选（点「筛选」按钮展开 / 收起）

只对**内容搜索**生效，勾选状态会被记住。全选或全不选都等于不筛选。

| 分组 | 选项 |
|---|---|
| 文字类型 | 单行文字 / 多行文字 / 标注 / 块属性 |
| 搜索位置 | 模型空间 / 图纸空间（布局） |
| 块定义范围 | **摆放的实体**：图上看得见、点得到的内容，含块实例里显示的属性值，日常勾这个就够<br>**块定义内部（含嵌套块）**：块模板自带的固定文字，块插入多少次只算一份 |

---

## 3. 结果、预览与缩略图

- **文字预览**：显示这张图纸提取到的文字，内容会随「筛选」的勾选变化。右上角浮条显示字数，`‹ ›` 可跳到上一个 / 下一个命中。
- **图纸缩略图**：读取 CAD 保存 DWG 时**嵌在文件里的预览图**，不需要安装 CAD。这张图本身不大（常见约 256 像素宽），属于正常现象。以下情况会提示"没有内嵌预览图"：保存时没有生成缩略图，或文件版本过旧。
  可在「设置 → 显示图纸缩略图」关闭；预览区中间的分隔条可上下拖动调整高度，位置会被记住。
- **双击**一行：用系统默认程序打开图纸。
- **右键菜单**：全选 / 打开文件 / 打开文件所在位置 / 复制文件路径 / 复制文件 / 批量替换所选…
- **重命名**：选中一行后按 `F2`（或再点一次已选中的文件名），输入新名字回车。**这会直接改磁盘上的真实文件名**；重名或含非法字符时会提示并拒绝。
- **导出**：「文件 → 导出列表」（`Ctrl+E`），保存为 CSV。
- **列显示**：「设置 → 显示列」可选文件名 / 文件路径 / 创建日期 / 修改日期 / DWG 版本 / 大小。点表头可排序。

---

## 4. 批量替换

点菜单栏「**文字替换**」进入替换页（点「搜索」切回，切换不会打断正在运行的任务）。

**进入方式：** ① 在搜索结果里选中若干行 → 右键「批量替换所选…」，文件自动带过去；② 直接进替换页，用「添加文件… / 添加文件夹…」自己加。

**操作步骤：**

1. 在「替换内容」里填「旧文字 → 新文字」，可点「+ 添加一组」填多组。
2. 选**类型**：单行文字 / 多行文字 / 标注（只改手动覆盖过的标注文字，自动测量值不受影响）/ 块属性（块实例上已填好的属性值，如图框里的图号、比例）。
3. 选**位置**：模型/图纸空间（默认）；「块定义内部」会连块模板一起改，**影响所有用到该块的地方，请谨慎**。
4. 选**引擎**（见下表）。
5. 点「**预览**」先看会命中哪些内容（不保存文件，可选但建议）。
6. 点「**确认执行**」真正写入。**执行前会自动备份原文件。**

| 引擎 | 需要 AutoCAD | 特点 |
|---|---|---|
| accoreconsole（推荐） | 是 | 不占用你正在开着的 AutoCAD 窗口。探测不到时，在「高级设置…」里手动指定 accoreconsole.exe 路径 |
| AutoCAD COM | 是 | 整批任务共用一个隐藏的 AutoCAD 实例；启动时会有一瞬间抢输入焦点 |
| ACadSharp | 否 | 第三方开源库，速度快，但个别文件有出错风险 |

**恢复：** 点「从备份恢复…」→ 选备份批次 → 勾选要恢复的文件 →「恢复所选」。可勾选「覆盖原路径已存在的文件」。备份位置可更改，默认自动保留最近 20 个批次，超出的旧批次会在每次执行后自动清理。

> 建议：重要图纸先拿副本试一遍；执行前先关闭正在 AutoCAD 里打开的这些图纸。

---

## 5. 书签

把常用的「文件名 + 内容」搜索条件存起来。

- **添加**：点文件名框里的书签图标（空心 = 未收藏，实心 = 已收藏），或菜单「书签 → 添加到书签」。
- **使用 / 管理**：菜单「书签 → 书签管理」，可应用、重命名、删除。

---

## 6. 索引与数据管理

**索引范围：** 默认覆盖本机所有**固定硬盘**（系统盘最后扫）。网络盘、U 盘、光驱不会自动扫描——把它的目录添加为搜索目录并勾选，再点搜索，就会被优先索引。索引之后，新增和修改的图纸由实时监控自动更新。

**「设置 → 索引管理…」** 里可以：

| 按钮 / 项目 | 作用 |
|---|---|
| 刷新信息 | 更新已索引图纸数、数据库大小、最后更新时间 |
| 存储路径 | 更改索引数据库的存放位置 |
| 排除目录管理 | 让某些目录不参与索引 |
| 查看失败文件 | 列出内容提取失败的图纸（损坏、格式不支持、解析超时）。这些图纸**仍能按文件名搜到**，只是搜不到里面的文字 |
| 整理数据库空间 | 只回收磁盘空间，不删除任何索引 |
| 内容提取并发数 | 调大更快但更占 CPU，电脑变卡就调小；下一次索引时生效 |
| 清空并重建索引（红色） | 清空后重新提取，**耗时可能很长**，平时不需要 |

---

## 7. 设置、快捷键与后台运行

**「设置」菜单：** 索引管理… ／ 语言 ／ 集成到右键菜单 ／ 最小化为系统托盘图标 ／ 随系统自启动 ／ 显示图纸缩略图 ／ 显示列 ／ 打开数据存储目录

- **集成到右键菜单**：资源管理器里右键文件夹，出现「用 DWG 图纸搜索工具搜索此目录」。
- **最小化为系统托盘图标**（默认开启）：点窗口关闭按钮时缩到托盘，后台索引继续；双击托盘图标恢复窗口。**要彻底退出，请右键托盘图标选「退出程序」**；取消勾选此项后，点关闭按钮就是真正退出。
- **随系统自启动**（默认关闭）：开机后在后台启动，只显示托盘图标。

| 快捷键 | 功能 |
|---|---|
| `Ctrl+N` | 新建搜索（清空关键词、结果和预览，保留目录勾选） |
| `Ctrl+Shift+N` | 新建窗口（独立窗口，共用同一份后台索引） |
| `Ctrl+E` | 导出列表 |
| `Ctrl+Q` | 退出程序（与点窗口关闭按钮效果相同：默认最小化到托盘，取消勾选「最小化为系统托盘图标」后为真正退出） |
| `F2` | 重命名选中的文件 |
| `Ctrl+V` | 在目录栏里粘贴路径 |

**「帮助」菜单：** 关于 ／ 捐赠作者 ／ 检查更新（有新版本时菜单上会出现小红点）／ 程序日志

---

## 8. 常见问题

**刚保存的图纸或刚添加的目录搜不到？**
看状态栏是否还在「后台索引中」，索引没跑完时结果可能不全。添加并勾选目录后点搜索，该目录会被优先索引。

**文件名能搜到，内容却搜不到？**
这张图可能内容提取失败，到「索引管理 → 查看失败文件」里确认。

**预览框提示"当前筛选条件下，该图纸没有匹配的文字内容"？**
可能是「筛选」勾选太窄，或这张图还没建立文字索引（重新点一次搜索会触发增量扫描）。

**搜索时一直提示"关键词至少需要2个字"？**
单个字符会被忽略，文件名里的 `*` `?` 也不计入字数（所以单独一个 `*` 不能搜）。请至少输入 2 个字符，或改用正则（正则不受长度限制）。

**出了问题怎么排查？**
「帮助 → 程序日志」可打开日志文件；反馈问题时附上日志会更容易定位。
