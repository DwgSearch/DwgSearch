# 更新日志

所有重要变更均记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [2.19.0] - 2026-09-03

### 新增

- 建索引时读取 DWG 版本号并存入数据库（新增字段+迁移逻辑），搜索时直接读库，不用再逐个现读文件

- **全新发布托管模式**：项目迁移至 GitHub Releases 分发，仅提供二进制下载，源码不公开
- **便携版分发**：新增免安装 ZIP 包，解压即用，支持 U 盘携带、多版本共存
- **SHA256 校验文件**：每版本发布附带 `SHA256SUMS.txt`，防篡改验证
- **专有许可证**：明确授权范围、禁止逆向、免责条款

### 改进
- 搜索结果表格架构从 QTableWidget 迁移到 QAbstractTableModel + QTableView，填充耗时不再随命中数量线性增长
- 表格填充方式优化为一次性批量装载，不再逐行创建控件对象
- 内联改名逻辑整合进数据模型层，改名失败自动回退

### 修复
- "大小"列无法隐藏的限制（原限制依据不成立，已放开）
- F2 改名快捷键编辑区域被冻结列遮挡、编辑框不可见的问题
- 选中行后直接打字即触发改名的误触发风险（保留 F2 和点击已选中行两种主动方式）

### 内部变更
- PyInstaller 打包配置分离：`build.spec`（单文件夹模式）、`build_onefile.spec`（单文件模式）
- .NET 子程序统一目标框架 `net48`，精简依赖
- 移除打包产物中的 PDB 符号文件，减小分发体积

---

## [2.18.0] - 2026-08-29

### 新增
- 帮助菜单新增"捐赠作者"入口，微信/支付宝二维码并排展示
- 批量替换新增 ACadSharp 引擎（第三种方式，本机无需安装 AutoCAD）
- 搜索结果表格支持右键表头/设置菜单自定义显示/隐藏列
- 搜索结果表格新增"DWG版本"列

### 改进
- 捐赠二维码图片改为代码内嵌（base64），避免被随意替换

- AutoCAD COM 引擎启动前增加预检测，未安装时给出更明确的中文提示

- 批量替换的引擎选择由勾选框改为下拉菜单（三选一）

- "DWG版本"列默认隐藏，隐藏时自动跳过多余的文件读取，不拖慢搜索

- 搜索结果详情（创建/修改时间、大小、DWG版本）改为后台线程分批加载，避免结果量大时界面卡死

### 修复
- 修复标题栏版本号仍显示旧版本号的问题

- 修复 ACadSharp 引擎写入文件时因文件被自身占用导致的写入失败

- 修复"隐藏列"设置无法被记住、容易被其他设置项保存操作覆盖丢失的问题

- 修复实时监控偶发异常导致整个监控线程静默退出、且不会自动恢复的问题

---

## [2.17.0] - 2026-08-24

### 新增
- 菜单栏"设置"新增"最小化为系统托盘图标"选项（默认开启）和"随系统自启动"选项（默认关闭）

- 新增"书签"功能：菜单栏"书签"菜单（添加到书签/书签管理）、文件名搜索框旁书签按钮、书签管理弹窗（应用/重命名/删除）

- 书签按钮支持"已收藏"状态显示（实心图标），当前搜索条件匹配已有书签时自动点亮

- 搜索目录标签栏支持拖拽调整标签顺序

- 右键菜单"用 DWG 图纸搜索工具搜索此目录"新增图标显示

- 扩展 AutoCAD 插件工程（AccoreconsolePlugin）编译配置，支持 2018–2027 全版本年份（2025 前用 net48，2025 起用 .NET Core 对应版本）

### 改进
- 书签新增防重复机制：相同搜索内容提示已收藏、相同名称禁止重复添加

- AccoreconsolePlugin 工程注释完善，明确 `AutoCADInstallDir` 可指向完整 AutoCAD 或单独的 ObjectARX SDK，无需为编译安装完整 AutoCAD

### 修复
- 修复右键菜单"搜索此目录"打开的新窗口被其他窗口遮挡、需手动从任务栏查找的问题（前台窗口抢占）

---

## [2.16.0] - 2026-08-22

### 新增
- 两个搜索框中间的可拖拽分隔线（记忆上次拖动的位置）

- 放大镜/正则".*"/清空"×"/下拉箭头四个图标的"点击热区""高亮色块""图案本身"三项尺寸均可独立设置，集中写在函数开头"尺寸设置区"并做了说明注释

### 改进
- `DwgTextExtractor.exe` 从 .NET 8 自包含改成 .NET Framework 4.8 框架依赖，体积从 30+MB 降到几MB

- 两个主搜索框边框颜色跟界面其它元素统一，聚焦时高亮

- 正则开关/清空按钮从内嵌小图标改成独立叠加按钮，摆脱尺寸限制

### 修复
- 直径符号 `⌀`/`∅`/`Ø`/`ø` 编码不统一，导致搜索只能命中其中一种写法

- 右侧预览区高亮跟着一起漏检（同一个直径符号编码问题连带导致）

- "查找替换"功能因原始存储编码不统一，两份图纸只能替换一份

- `DwgTextExtractor.exe` 崩溃(如缺依赖DLL)时被静默当成"提取成功但无内容"，不会出现在失败文件列表

- 下拉框右侧箭头黑边、以及后续箭头消失的问题

- 右键菜单启动时 `config.json` 被写到目标目录而不是程序自身目录

- 软件未打开时，右键菜单搜索会同时弹出两个窗口

---

## [2.15.0] - 2026-08-20

### 新增
- accoreconsole.exe 三层探测：注册表 → 盘符扫描 → 手动指定路径，支持在"accoreconsole 高级设置"弹窗里手动指定/清除

- 批量替换执行前自动备份：按时间戳分批次、镜像原始目录结构存放，不同来源同名文件不会互相覆盖

- "从备份恢复"功能：可选批次恢复、删除批次、自动保留数量清理

- 备份恢复列表增加"文件名"列，双击可打开文件所在位置

- "文字替换"对话框文件列表、主界面搜索结果列表均支持列宽记忆

- "文件"菜单新增"新建窗口"，可同时打开多个独立搜索窗口

- 新增右键菜单集成："设置"里可开启/关闭，开启后资源管理器右键文件夹可直接用本工具搜索该目录

### 改进
- 替换引擎去掉"测试中"提示文字

- 有实时监控（watchdog 可用）时跳过替换后的重复索引重建，避免同一批文件被处理两遍

- 预览与执行解耦，不跑预览也能直接确认执行（有执行前备份兜底）

- 按钮文案简化："预览"/"确认执行"/"取消"，"从备份恢复"移到操作按钮行

- 备份保存位置、保留数量等设置统一收进"从备份恢复"弹窗，主界面精简

- accoreconsole 手动路径设置收进独立的"高级设置"弹窗，不常驻主界面

- 备份保留数量改为输入即生效，去掉多余的"保存"按钮

- 多窗口架构重做：新窗口在同一进程内创建、共用同一个索引管理器和系统托盘图标，不再是各自独立进程（效果对齐 Everything / Anytxt Searcher）

### 修复
- 列宽拖动后如果很快关闭窗口/软件，防抖保存来不及触发导致列宽丢失的问题（主界面表格 + 替换对话框表格均已修复）

- 替换对话框"当前状态"栏遇到超长文本（如报错信息）会把整个窗口撑宽变形的问题，改为自动省略号截断+悬停查看完整内容

---

## [2.14.0] - 2026-08-17

### 新增
- 顶部菜单栏:文字替换(直接触发)、文件(新建搜索/导出列表/退出程序)、设置(索引管理/语言/打开数据存储目录)、帮助(关于/检查更新/程序日志)

- "新建搜索"功能:一键清空两个搜索框和当前结果列表

- "导出列表"功能:把当前结果表格(含排序后的顺序)导出为 CSV

- 程序日志落盘:原来只打印到控制台的日志,现在同时写入磁盘文件(带自动轮转),菜单栏"帮助→程序日志"可直接打开

### 改进
- 表格上方原来的"文字替换工具"/"索引管理..."按钮去掉,功能收进新菜单栏,避免重复入口
- 右侧预览区"字数/上一个/下一个"从占一整行,改成悬浮在文本框右上角的小工具条,不占用/不显示时自动隐藏,文本框能顶到面板最上面

### 修复
- 文件名列表头排序只能降序、切不回升序
- 排序箭头会一直挂在文件名列上，切到别的列排序时不会消失
- 点搜索或按回车后，搜索框会莫名其妙失去焦点
- 双击/右键打开 DWG 文件时跳转到"文档"目录，而不是正常打开(explorer.exe 单实例转发失败)
- 改成 os.startfile() 后又导致打开文件时整个程序闪退(shell 扩展在进程内崩溃)，最终改为用独立的 PowerShell 进程打开文件，两个问题一起解决
- 改变窗口大小时，搜索框里的图标(放大镜/清空/正则)会跑偏错位
- 正则开关图标选中态的高亮背景被拉伸变形、占满整个输入框高度不美观；连带撤销了"图标点击范围撑满整框高度"这个改动，退回原生小图标
- 自定义下拉箭头点一下只会展开、不会再点一下收起，且样式不像系统原生的；改回原生 QComboBox 自带的下拉箭头
- 右侧预览浮层在内容变化(字数、命中数位数变化)时不会重新计算自身宽度和位置，导致文字被裁切、显示不全
- ".*" 图标的圆点画得过大、和星号线条粗细比例失衡，视觉上显得变形(这一项优化方案还在调整比例、尚未最终定稿)

## [2.13.0] - 2026-08-14

### 新增
- 批量文字替换支持一次填写多组"旧文字→新文字"，按顺序链式替换，不再只能一对一
- 替换任务的文件列表新增序号、状态（待处理/成功/无匹配/失败）、命中数四列，处理时自动滚动到当前行
- 搜索目录改为可勾选的多目录标签栏，支持同时按多个目录做"或"搜索、悬停删除、点击选中再点编辑路径、双击用资源管理器打开、点击空白处或粘贴剪贴板路径快速添加
- 文件名/内容搜索框新增正则表达式开关，可按正则搜索，命中结果的高亮同步支持正则匹配
- 搜索结果表格"文件名"列改为冻结列，横向滚动时固定不动；支持像 Windows 资源管理器一样单击选中、再点一下改名（自动跳过扩展名选中范围），也支持点表头排序

### 改进
- "上一个/下一个"导航按钮改成夹住计数文字的排列方式，计数文字精简
- 替换工具的详细日志默认收起，改用常驻状态行显示最新进度
- 搜索框内的放大镜、正则开关图标尺寸随框体高度等比例缩放
- 筛选下拉里过长的说明文字支持自动换行，不再挤成一整行

### 修复
- 目录标签栏边框和背景不显示的问题
- 目录标签的删除按钮悬停状态显示错误（一直常亮，未跟随鼠标位置）
- 多目录搜索时的目录归属判断错误（字符串前缀误判，如误将"C:\FooBar"归入"C:\Foo"）
- 冻结列一系列显示与交互问题：边框颜色不一致、选中高亮色不一致、横向滚动后被自动复位、拖宽表头会遮挡而非推挤其它列、文件名列一度完全消失、右键菜单失效、超长文件名不显示省略号

---

## 版本号规则

采用 **主版本.次版本.修订号** (`MAJOR.MINOR.PATCH`)：

| 类型 | 规则 | 示例 |
|------|------|------|
| **主版本** | 架构重写、破坏性变更、核心引擎更替 | 2.x → 3.0 |
| **次版本** | 新增核心功能、重要 UI 交互变更 | 2.18 → 2.19 |
| **修订号** | 仅修复 Bug、微调、内部优化 | 2.19.0 → 2.19.1 |

---

## 旧版本归档

| 版本 | 发布日期 | 下载 |
|------|----------|------|
| 2.18.0 | 2025-07-15 | [Release](https://github.com/DwgSearch/DwgSearch/releases/tag/v2.18.0) |
| 2.17.0 | 2025-06-01 | [Release](https://github.com/DwgSearch/DwgSearch/releases/tag/v2.17.0) |
| 2.16.0 | 2025-04-20 | [Release](https://github.com/DwgSearch/DwgSearch/releases/tag/v2.16.0) |
| 2.15.0 | 2025-03-01 | [Release](https://github.com/DwgSearch/DwgSearch/releases/tag/v2.15.0) |
| 2.10.0 | 2024-12-01 | [Release](https://github.com/DwgSearch/DwgSearch/releases/tag/v2.10.0) |

> 注：v2.19.0 以前的版本仅作历史参考，建议始终使用最新版本。